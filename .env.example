# Docker Compose local development settings.
# Copy to .env before running docker compose.

POSTGRES_USER=loreum
POSTGRES_PASSWORD=loreum_local_pw
POSTGRES_DB=loreum
DB_PORT=5432
REDIS_PORT=6379

# Optional Docker Compose overrides.
CONTAINER_SUFFIX=
OPENSEARCH_PORT=19200
OPENSEARCH_DISABLE_SECURITY=true
OPENSEARCH_ADMIN_PASSWORD=LoreumLocalDev123!
